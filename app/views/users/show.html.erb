<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if @user.microposts.count > 0 %>
  <h2> First comment: </h2>
  <strong> <%=@user.name %> said: </strong> <%= @user.microposts.first.content %> <br>
  <strong> In this moment: </strong> <%= @user.microposts.first.created_at %> <br>
<% end %>
<br>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
